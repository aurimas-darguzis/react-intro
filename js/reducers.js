const DEFAULT_STATE = {
  searchTerm: ''
}

const rootReducer = (state = DEFAULT_STATE, action) => {
  switch (action.type) {
    // case: SET_SEARCH_TERM 

    
    default: 
    return state
  }  
}

export default rootReducer